# api-service

API gateway that sits between the dashboard (frontend) and all backend microservices. Authenticates requests and proxies them downstream.

## Proxy Convention

api-service is a **transparent proxy**. It authenticates, applies middleware, and forwards requests — it does NOT redefine, rename, or transform downstream routes.

**"Transparent" does NOT mean "generic catch-all".** Every downstream endpoint requires its own explicit Express handler in `src/routes/<service>.ts`, its own Zod schema in `src/schemas.ts`, and a re-generated `openapi.json`. There is no `app.use("/v1/*", genericProxy)` and adding one is out of scope for any normal feature PR — it would break OpenAPI auto-generation, per-route auth-tier enforcement, query-param whitelisting, and ~30 existing `*-proxy.test.ts` files. If you think you need a generic catch-all, that is a standalone architectural proposal, not a "while we're here" tweak.

"Transparent" means: no path rename, no aggregation, no body transform, no field stripping, no header injection beyond the standard identity headers. It does NOT mean fewer files.

### Rules

1. **No path renaming.** The path sent to the downstream service must match the actual route on that service. Always check the API registry (`mcp__api-registry__list_service_endpoints`) for the correct path before writing a proxy route. When a downstream path is renamed (e.g. `auto-reload` → `auto_topup`), the api-service-facing path must be renamed in lockstep — do NOT keep a legacy client alias pointing at the new downstream path. The dashboard PR catches up; cross-repo coordination is mandatory, not optional.

2. **No aggregation logic.** If a response needs enrichment from multiple services, that logic belongs in the backend service, not here. Do NOT build ad-hoc enrichment functions that call multiple services and stitch results together.

3. **Correct middleware per route tier:**
   - Downstream `/orgs/*` routes → `authenticate + requireOrg` (client must provide org context)
   - Downstream `/internal/*` routes → NEVER exposed to clients. api-service may call them server-side when needed, but they are not mounted as client-facing routes.
   - Downstream `/public/*` routes → **no middleware at all**, not `authenticate`. A downstream route that is public at the producer is public here: the consumer is an anonymous visitor (the marketing site is generated from these reads), so the gateway must not demand a key, an org or a user, and must not invent identity headers for them. `src/routes/features.ts` is the reference — its public block is mounted both unprefixed and under `/v1`, and every handler calls `callExternalService` with no `buildInternalHeaders`. Assert the absence behaviourally: drive the real router with supertest and check the forwarded request carries no `x-org-id` / `x-user-id`, and do NOT mock `authenticate` in that file — mocking it would hide a gate rather than prove there is none. (`tests/unit/public-channels-proxy.behavior.test.ts`, PR #842.)

4. **No body transforms.** Don't strip fields from the body or inject fields from headers. Just proxy the request as-is.

5. **No path invention.** If a downstream service doesn't have the route, api-service must NOT invent it. If a route was deprecated upstream, remove the proxy route here too.

6. **No shape assertions on pass-through responses.** Billing and any other endpoint whose response schema is `z.object({}).passthrough()` is owned by the downstream service. Do NOT write api-service unit tests that assert specific field names or types on those responses — they only re-encode the downstream contract here and force coordinated edits on every downstream rename. Tests must assert (a) the proxy forwarded to the correct downstream path, and (b) the upstream body was forwarded byte-identical. That is the entire contract.

7. **No sanitizing upstream error bodies.** `callExternalService` (`src/lib/service-client.ts`) throws `Error.message = <upstream body verbatim>` on non-2xx; routes surface it to the client via `res.json({ error: err.message })`. Do NOT re-introduce a generic `Service call failed: <status>` mask, do NOT JSON-extract only the `error` field, do NOT truncate. Downstream services are ours, behind the same trust boundary as the gateway — PII / stack-trace hygiene is enforced at the downstream layer, not by double-masking here. LLM/MCP callers (dashboard agent, MCP tool callers) cannot debug without the real upstream message. This rule supersedes PR #437 finding S5; the security audit was over-cautious for an internal microservice mesh. Hotfix v0.41.2 (PR #462) restored passthrough after agents hit opaque `{"error":"Service call failed: 500"}` from `/workflows/upgrade`.

   **Corollary — a route catch MUST NOT rebuild its own envelope out of `err.message`; use `respondUpstreamError` (`src/lib/upstream-error.ts`).** `err.message` IS the upstream body, so `res.status(err.statusCode || 500).json({ error: err.message })` stringifies the WHOLE downstream JSON body into the `error` string: every machine-readable field (`code`, `details`) is destroyed, no consumer can branch on the reason, and the dashboard renders the raw body to the end user. Forwarding the status while flattening the body is NOT passthrough — the body is part of the contract too (rule #8: downstream owns the shape). `respondUpstreamError(res, error, "<fallback>")` re-emits the upstream JSON object field-for-field under the upstream status, and wraps a non-JSON body as `{ error: <body> }`. Applied to the brand routers after a prod incident where adding a website rendered `Could not save: {"error":"A brand already exists for domain \"steadyrecruitment.com\"","code":"DOMAIN_CONFLICT"}` in the dashboard. Note the doc-comments on those routes had claimed "the 409 propagates verbatim" for months — a claim no test checked, because the tests asserted only the status. **A passthrough test MUST assert the BODY, not just the status.**

   **Corollary 2 — a proxy test MUST assert the FULL downstream path literal, never a prefix substring.** `expect(block).toContain("/internal/orgs/")` plus `expect(block).toContain("/invites/claim")` passes for `/internal/orgs/{orgId}/invites/claim` — a path the downstream never served. A prefix is satisfied by every wrong path built from the right pieces, which is exactly the shape a hand-written proxy route gets wrong: an org-scoped path invented for a downstream route that is not org-scoped. Assert the string the route actually sends, quotes included — `expect(block).toContain('"/internal/invites/claim"')` — plus a negative on the shape it must NOT be. Better still for a new route, drive it through supertest and assert the captured `fetch` URL; a source-substring test cannot see a template literal's interpolated value at all. And **a route with no live caller gets a behavioural test, not a lighter one** — no consumer means no one will report the 404 for you. Hotfix v0.93.1 (PR #774): `POST /v1/orgs/:orgId/invites/claim` pointed at `/internal/orgs/{orgId}/invites/claim` from the day it shipped, while client-service serves `POST /internal/invites/claim` and identifies the claiming org from the body. The dashboard's referral card was decorative copy with no backend, so nothing ever hit it and the prefix assertions stayed green the whole time.
   **Corollary 3 — a test that reads a route file as TEXT cannot verify what goes over the wire. Any claim about IDENTITY or AUTH needs a supertest assertion.** Many `*-proxy.test.ts` files here `readFileSync` the router and assert substrings (`expect(content).toContain('"/billing/x"')`, `expect(middlewares).toContain("requireOrg,")`). That is fine for cheap wiring checks — is the route mounted, is the downstream literal right, is `respondUpstreamError` present — and it is worthless for the two things that actually matter on an org-scoped proxy: that the `x-org-id` reaching downstream is the AUTHENTICATED one (a caller-supplied `x-org-id` / `?orgId` must not override it), and that an unauthenticated call is refused. `requireOrg` appearing in the source proves neither; both hold vacuously no matter what the handler sends. So when a route's security property is "the caller cannot name whose data to read", the PR must drive the real router with `supertest` + a stubbed `global.fetch` and assert the forwarded URL, the forwarded identity headers, and the byte-identical body — plus a separate no-auth-mock file for the 401/400 gate (mock `authenticate` and you are testing the stub). Precedent: PR #779 shipped `GET /v1/billing/free-credit-promises` with source-string tests only; #781 added `tests/unit/free-credit-promises-{behavior,auth}.test.ts` as the reference shape.

8. **Response schemas are passthrough by default — no field declaration.** Every proxied response schema in `src/schemas.ts` MUST be `z.object({}).passthrough().openapi("<Name>")`. Do NOT re-declare downstream fields. Downstream owns the shape; api-service forwards bytes. Field rename downstream = zero edit here. Three precedents codify this: billing PR #454 ("collapse billing response schemas to passthrough"), runs PR #455 (response schema correction after re-declaration drift), brand contract change (this rule's birth). The ONLY exception is a response shape api-service itself constructs via aggregation (e.g. `BrandRunsResponse` enriches with `runs-client` cost data) — those are typed accessors, and the route handler MUST cite the construction site in a comment. If a typed response schema exists and no aggregation justifies it, collapse it to passthrough — that is always safe.

   **Corollary — the REQUEST body is a passthrough too, and a whitelist there silently DROPS fields instead of merely mis-documenting them.** Rule #8 above is about response schemas; the same ownership applies inbound, with a worse failure mode. A request schema that re-declares a subset of what the downstream accepts and then forwards `parsed.data` sends the downstream a body with the undeclared fields MISSING — a create/update that looks well-formed here and is rejected, or silently mis-configured, there. So a proxied request schema declares ONLY the fields the gateway itself needs (a required-field check it 400s on, an id it resolves, a slug it derives a header from, a body it key-presence-validates) and carries `.passthrough()` for everything else. Two corollaries of the corollary: never add a field to the whitelist just to let it through — that leaves every sibling field still stripped, and the fix is one `.passthrough()` on the same schema; and never enumerate a downstream VOCABULARY here (`z.enum([...])` on a status/goal/key the downstream owns), because the gateway then 400s a value the downstream accepts and every token the downstream adds needs a gateway release. Downstream rejects what it does not accept and rule #7 forwards that rejection verbatim. Hotfix v0.98.1 (PR #812): `CreateCampaignRequestSchema` whitelisted its fields, so the `funnelKey` a sales campaign must state was dropped on the floor and campaign-service 400'd every sales campaign creation through the gateway, beta onboarding signups included; `dailyBudgetCents`, `startDate` and the `notify*` trio had been silently stripped the whole time, and `goal` was enumerated three ways while campaign-service took any non-empty string.

   **Corollary 2 — the QUERY STRING is a passthrough too, and it fails SILENTLY where a dropped body field at least gets rejected.** A GET handler that destructures a fixed set of parameters off `req.query` and rebuilds the upstream query with `new URLSearchParams()` is the same whitelist wearing different clothes: everything else the caller sent is dropped, and because a filter the downstream never receives simply does not narrow anything, the caller gets a **200 carrying the un-narrowed population** with no signal the parameter was ignored — worse than the body case, where the downstream usually 400s. Forward the raw query instead: `` `<downstream-path>${rawQueryString(req.originalUrl)}` `` (`src/routes/crm.ts`, `src/routes/leads.ts`, `src/routes/mailing-lists.ts` each carry the 3-line helper). Read it off `req.originalUrl`, NOT re-serialized from `req.query` — re-serializing imposes this gateway's opinion on repeated keys (`?ids=a&ids=b`), ordering and encoding. Read out of `req.query` ONLY what the gateway itself acts on (the required-parameter presence check it 400s on). The OpenAPI query object keeps documenting the known parameters and carries `.passthrough()`; it is a doc, not a whitelist. Hotfix v0.98.6 (PR #826): `GET /v1/leads` rebuilt the upstream query from `brandId/campaignId/limit/offset/view`, so lead-service's new population slice — and every other filter it accepts — could not be asked for through the gateway at all.

9. **SSE / streaming proxies MUST go through `streamExternalService` — never hand-roll a `fetch` + manual pipe.** `streamExternalService` (`src/lib/service-client.ts`) wires an `AbortController` to the client socket: it passes `signal` to the upstream `fetch` and calls `controller.abort()` on `expressRes` `"close"`, so a client that disconnects mid-stream (closed tab, navigation, network drop) tears down the upstream connection instead of leaking it. This is LOAD-BEARING: `server.requestTimeout = 0` (set for long SSE) means nothing else reaps an orphaned stream. A raw `fetch` + `reader.read()` loop without abort-on-disconnect keeps draining the upstream SSE for its full lifetime (chat sessions run 30–60 min); each orphaned reader pins native undici buffers that never free → RSS climbs until the container hits its memory limit → OOM-restart sawtooth. Diagnostic tell: multi-GB RSS on a "pure passthrough" = native buffers, NOT V8 heap (no `--max-old-space-size`, heap caps ~4GB). Hotfix v0.72.2 (PR #617) added the abort wiring after prod OOM-cycled — that was on Railway, but the leak is in this process, so it reproduces on any host. The campaign-stream poll loop (`src/routes/campaigns.ts`) is the one exception — it self-manages its own `req.on("close")` cleanup and does not use `streamExternalService`.

10. **A proxied response big enough to be measured in MB must be PIPED, not parsed.** `callExternalService` + `res.json(result)` holds two full copies of the body at once — the parsed object graph and its re-serialization — so a downstream that answers in the hundreds of MB kills the gateway with `FATAL ERROR: Reached heap limit ... JsonStringifier::SerializeString`. An OOM is not scoped to the request that caused it: the process dies and every org's in-flight request dies with it, which is why this is an availability bug, not a slow-endpoint bug. Use `pipeExternalService` (`src/lib/service-client.ts`) instead — it copies the upstream body stream into the Express socket via `pipeline` (so `res.write` backpressure throttles the upstream read), forwards only `content-type`, and aborts the upstream fetch when the client socket closes, exactly like `streamExternalService`. Raising `--max-old-space-size` only moves the cliff. The bytes the caller gets are the bytes the downstream sent, which is stronger passthrough than re-serializing (rules #4/#8); a non-2xx is still read to completion and thrown verbatim with its status (rule #7). Applied to `GET /v1/leads` in v0.98.4 (PR #821) after prod OOM-cycled 5 times: the largest brand returns 53,777 leads / 156 MB there and the dashboard polls it every 30 s per open tab. Reach for it on any route whose worst-case body is not comfortably small — a list endpoint over an unbounded per-brand or per-org collection.

11. **The QUERY STRING is a passthrough too — forward it verbatim off `req.originalUrl`, never rebuild it from a destructured whitelist.** A handler that reads a fixed set of params off `req.query` and reassembles a `URLSearchParams` from only those owns the downstream's parameter vocabulary, exactly like a whitelisted request body (rule #8's corollary), and it fails the same silent way: the caller gets a 200 carrying an unnarrowed result, with no error and no warning that its narrowing was discarded. Take the raw substring after the first `?` instead (`rawQueryString(req.originalUrl)` in `src/routes/leads.ts`) — repeated keys, ordering and the caller's own percent-encoding all survive byte-identical, and a parameter the downstream ships next needs zero edit here. The gateway's OWN guards still read `req.query`: a required-param check it 400s on is the gateway's concern, but the parameter's VALUE is not — an invalid one is the downstream's 400 to raise, and rule #7 forwards it verbatim. Document the params you know in the openapi schema, and say in the description that they are the ones documented today rather than a whitelist. Applied to `GET /v1/leads` in PR #826: the whitelist was `brandId, campaignId, limit, offset, view` — and `/orgs/leads` implements neither `limit` nor `offset`, so the list was not even an accurate picture of what the downstream accepted, while lead-service's new `status` (whose default now excludes the `skipped` rows, 82% of a large brand's payload) could not reach it at all, which would have emptied the staff console's Skipped / Buffered / Claimed tabs.

12. **A caller that wants a COUNT gets a count route, not the list route.** Piping (#10) and query passthrough (#11) both made `GET /v1/leads` survivable, but neither removed the download: a sidebar badge showing "57,622 leads" was still fetching every lead and calling `.length` on it, every 30 s per open tab. Streaming a body the caller throws away is a cheaper failure, not a fixed one — the fix is a route that answers the question asked. `GET /v1/leads/stats` → lead-service `GET /orgs/stats` is that route: 580 bytes / 200 ms against the brand whose list is 41.9 MB / 8.4 s (64,246 lead rows — 8,521 served, 1,962 buffered, 53,760 skipped, 3 claimed), measured in prod 2026-08-16. The counts were already computed upstream and this gateway already read them server-side for `GET /v1/campaigns/:id/stats`; the only thing missing was a client-facing way in — which is the general shape here, so before proposing that a surface derive an aggregate from a list, check `list_service_endpoints` for the aggregate the downstream already serves (rule #2 forbids computing it here either way). The route carries no `brandId`/`campaignId` guard, unlike the list route: that guard exists there to stop a caller accidentally asking for the org's whole lead population, and an unfiltered count is one small row.

13. **A caller holding ONE row's identity gets a single-record route, not a filter on the list route — and a `/:id` route is registered AFTER every literal sibling.** The other half of #12: a count route stops a badge from downloading the population, and a single-record route stops a DETAIL PANEL from doing it. A table-plus-panel surface takes the slim list for the table and asks for depth one row at a time; before `GET /v1/leads/:id` the dashboard leads page and the public report page each pulled the full projection for a whole brand (~57k rows, >100 MB) so that one panel could render on click. The id is the one a list row already carries, so the consumer needs nothing new, and the scoping query it listed with (`brandId`/`campaignId`) is forwarded verbatim per #11 — it decides which scope the downstream's overlay answers for, so the panel agrees with the row. The org boundary stays the authenticated `x-org-id` from `buildInternalHeaders`; the downstream scopes the lookup on it, so a row in another org is a 404 there, not a check here. One record is comfortably small, so `callExternalService` is right and #10's byte pipe is not. **Express matches a path parameter against any single segment, so `router.get("/leads/:id")` declared before `router.get("/leads/stats")` swallows it and sends the downstream `stats` as an id** — declare the literals first, and assert it: `tests/unit/lead-detail-proxy.behavior.test.ts` drives the real router and checks each sibling still reaches its own downstream path. Shipped in PR #835 against lead-service `GET /orgs/leads/{id}`, one commit after #833 added `/leads/stats` — the ordering hazard is exactly what a same-file sibling race produces.

### Agent-readiness invariants of the published document (operationId, rate limits, deprecation)

The gateway's public surface is consumed by LLM function-calling layers and coding agents, not only by humans reading `/docs`. Three properties are load-bearing for that audience and are enforced by `tests/unit/openapi-agent-readiness.test.ts` — they hold for operations added later, because they are applied by `postProcessOperations` in `src/openapi/document.ts` rather than typed by hand on each `registerPath`:

- **Every operation carries a unique `operationId`.** It is what a tool generator binds a function name to. It is derived losslessly from `method + path` (`GET /v1/campaigns/{id}/stats` → `getV1CampaignsByIdStats`), so it moves only when the route moves — which is already a breaking change for the caller. An explicit `operationId` on `registerPath` wins over the derived one; a collision **throws at generation time** rather than shipping two tools with the same name.
- **Every operation declares typed inputs and a typed response.** The correlation-id header parameter is added to every operation (public ones included); identity headers are added only where the operation authenticates. If a new operation cannot be typed without re-declaring a downstream shape, leave it passthrough and say so — rule #8 outranks the count.
- **The document is generated by `buildDocument()` (`src/openapi/document.ts`), not by the script.** `scripts/generate-openapi.ts` only writes the file. Assert on the built document, never on the generator's SOURCE TEXT: `tests/unit/openapi-auth-docs.test.ts` and `docs.test.ts` used to `readFileSync` the script and went red the moment it moved — which is the good outcome; the bad one is a source-substring assertion that stays GREEN against a file that no longer builds anything.

- **The published document is the CUSTOMER's document. It is built for the `"public"` audience and that is the default.** `buildDocument()` with no argument strips every operation a customer's own key cannot call, and `scripts/generate-openapi.ts` writes THAT — so the committed `openapi.json`, `GET /openapi.json` and `/docs` all describe the API a customer can actually use and nothing else. The criterion is mechanical and lives in `stripNonCustomerSurface`: an operation goes if it declares `apiKeyAuth` (it needs the shared platform secret) or carries a `STAFF_ONLY_TAGS` tag (it authenticates with a user key but answers 403 to a non-staff caller, so the declared scheme cannot tell it apart). The `apiKeyAuth` scheme itself and any tag left without an operation go too — a tag description is where "staff-only" and "API key auth" are spelled out. Removing an operation from the document does NOT remove its route: the 24 platform operations and the 6 staff mailing-list ones answer exactly as before, and `tests/unit/openapi-public-surface.test.ts` asserts every router is still mounted. Staff read the complete document at **`GET /internal/openapi.json`**, gated by `authenticatePlatform` and built on demand with `{ audience: "staff" }` — no second committed file, and it exposes nothing to a caller who could not already call those operations. The `@distribute.you/cli` generates its command surface from a document, so a staff CLI must fetch that URL with `X-API-Key` to keep `distribute ops`. Documenting `X-API-Key` was not merely noise: `authenticate()` branches on that header BEFORE it reads `Authorization`, so a customer who follows the document and sends both gets `401 Invalid admin key` on a request that would otherwise have worked. When you add a staff family gated by `authenticate + requireStaff` (bearer, not the platform key), add its tag to `STAFF_ONLY_TAGS` — the source scan in that test file fails the build if a new one appears unaccounted for. (PR #875.)

**Rate limiting (`src/middleware/rate-limit.ts` + `src/lib/rate-limit.ts`) is mounted globally in `src/index.ts`, before every route.** Two rules govern changes to it:

- **Never advertise a budget nothing enforces.** The `RateLimit*` headers are emitted only on a request the limiter actually counted; an exempt caller gets no headers at all, because a header claiming a budget that is not applied is a false statement an agent will pace itself against.
- **The exemptions are what keep the fleet up, and both are resolved with zero network calls.** A valid `X-API-Key: <ADMIN_DISTRIBUTE_API_KEY>` (every internal service and both first-party dashboards — see `authenticate` path 1) is exempt; so is a request that arrives with no `cf-connecting-ip` / `x-forwarded-for` / `x-real-ip`, which means it never crossed the edge and therefore came from inside the docker network (a sibling service, the deploy health check). An INVALID `x-api-key` is NOT exempt — it falls through to the anonymous per-IP budget, so a guess cannot buy a bypass. If you add an exemption, add the test that proves the fleet path stays unthrottled.
- **The counter store is in-process on purpose.** There is no Redis/Postgres behind it, so there is no store that can be unavailable and therefore no fail-open-vs-fail-closed choice to get wrong: the limiter is exactly as available as the process it protects, and the numbers it reports are always true of the instance answering. Do not "improve" it into a shared store without deciding, in writing, which way it fails when that store is down.

**Deprecation is signalled, never silent (`src/lib/deprecation.ts`).** Nothing is deprecated today. When something is: mark it `deprecated: true` in `registerPath`, call `signalDeprecation(res, …)` in the handler so every response carries `Deprecation` (RFC 9745) + `Sunset` (RFC 8594) + `Link; rel="deprecation"`, and honour `MINIMUM_NOTICE_DAYS` between the two dates (`honoursMinimumNotice` checks it). Removal ships behind a new URL version prefix — `/v1` never changes meaning under a live caller. The policy is published twice for the two audiences: prose in `info.description`, and machine-readable at the document root under `x-deprecation-policy`.

### Staff / admin gating — `authenticatePlatform` + `requireStaff`

`authenticate` does NOT distinguish a staff user from a normal customer (it only sets `authType: "admin" | "user_key"`), and **`authenticatePlatform` alone is NOT a staff gate** — the platform key (`ADMIN_DISTRIBUTE_API_KEY`) is shared with the customer dashboard's server-side proxy, so `authType === "admin"` is true for ordinary customer traffic too. Two mechanisms, pick by what the route exposes:

- **Pure platform op, no customer-distinguishing needed** (`/admin/*`, internal ops that any admin-keyed caller may hit) → **`authenticatePlatform`** (`src/middleware/auth.ts`): requires `X-API-Key === ADMIN_DISTRIBUTE_API_KEY`, else 401. Precedent: `src/routes/promo-codes.ts` (PR #538).
- **STAFF-ONLY** (mutating grants, cross-org ledgers, fleet-wide ops data the customer dashboard must NOT reach) → **`authenticatePlatform` + `requireStaff`** (or `authenticate + requireOrg + requireStaff` when org context is also needed). `requireStaff` (`src/middleware/auth.ts`) requires `authType === "admin"` AND a forwarded `x-email` in the `STAFF_EMAILS` allowlist — the email is the staff signal the shared platform key can't provide. Non-staff → 403. Precedents: `src/routes/credits.ts` (`POST /v1/billing/credits/grant`, `GET /v1/billing/credits/grants/all`), `src/routes/instantly.ts` (`GET /v1/instantly/audit/sending-forecast`, PR #632), `src/routes/platform-uploads.ts` (`POST /v1/platform-uploads` → cloudflare-service `POST /internal/upload/base64` — a staff-uploaded PLATFORM asset with no org owner and no org billing; `requireOrg` is deliberately absent because the downstream route takes no org at all, so an org would scope and bill nothing).

Do NOT invent a new staff-role flag. Do NOT gate a staff-only route with `authenticatePlatform` alone — a customer's dashboard session carries the same admin key, so it would leak. Use `requireStaff` for the staff signal.

### Identity-enrichment headers forwarded on `/internal/resolve`

On the admin path, `authenticate` resolves `x-external-org-id` / `x-external-user-id` to internal UUIDs via client-service `POST /internal/resolve`, and forwards four OPTIONAL enrichment headers into that body when present: `x-email` → `email`, `x-first-name` → `firstName`, `x-last-name` → `lastName`, `x-org-slug` → `orgSlug`. The dashboard/admin proxy reads all four off the caller's session (Next.js `auth()` — `sessionClaims` for the profile fields, `orgSlug` for the slug) and sends them on every `/v1/*` request.

Rules for this set:

- **Forward verbatim, never derive.** api-service does not mint, normalize, slugify, or default any of these. It is the identity provider's value or nothing. An absent header means an absent body field — never an empty string, never a fallback.
- **Absent must stay harmless.** Every one of these is optional downstream; a caller that sends none still authenticates identically. Do not make any of them required, and do not 4xx on absence.
- **client-service owns the write semantics, not the gateway.** `orgSlug` in particular is written only when the stored `orgs.slug` is NULL (`COALESCE(orgs.slug, $orgSlug)`) — client-service guards it because upstream slugs are immutable per org, so every authenticated request self-heals an org that predates the header. Do NOT add a second "only if empty" opinion here.
- **Why the slug matters:** `orgs.slug` IS the org's invite/referral code — client-service `/invites/validate` and `/orgs/:orgId/invites/status` read it directly. Before this header existed, no org had a slug and the whole referral surface was dead (`code: null` for every org). Adding a new enrichment header follows the same shape; changing how any of them is derived is a downstream (client-service) contract change, not a gateway one.

### Retired downstreams — do NOT re-add routes for these

The Hetzner migration (2026-08-07/08) moved 24 services; the rest exist nowhere, and Railway is deleted. A gateway route whose only downstream is one of them answers `500 {"error":"fetch failed"}` (DNS resolution failure inside the docker network), which reads as an outage rather than as "this is gone". So no route here may point at a service that has no container on the box.

**Removed (v0.98.0, features retired by the owner)** — `ahref-service`, `articles-service`, `google-service`, `journalists-service`, `outlets-service`, `press-kits-service`. Their routers, schemas, `externalServices` entries and openapi paths are gone. `apify-service` and `twilio-service` are also dead but this gateway never routed to them. Do NOT re-add any of these, and do NOT re-add an `externalServices` entry for them — a proxy route with no live downstream is worse than no route.

**`crm-service` was in that list and is NOT any more — it was deployed to the box on 2026-08-12 and its routes were restored (`src/routes/crm.ts`).** The retirement test is "does this service have a live container on the box", not "was it dropped once": a downstream can come back, and when it does the route comes back with it. It now carries two contact sources behind one registry — the original CSV upload ingest and a Matrix DM ingest (WhatsApp / Telegram / Discord bridged into the homeserver on the box) — so the proxied surface is `/v1/orgs/contacts/*` AND `/v1/orgs/matrix/*`. Its `/internal/*` routes (`contacts/promote`, `matrix/sync`, `matrix/rebuild`) stay unproxied: they are cron-driven service-to-service calls, not dashboard paths (rule #3).

**Still routed, still 500ing, retirement NOT yet decided** — `ai-visibility-score-service` (`src/routes/visibility.ts`), `expert-quotes-requests-service` + `journalists-quotes-service` (`src/routes/quotes.ts`). These are the `ai-visibility-scoring` and `pr-expert-quote-outreach` features, both with campaigns in the last 30 days; whether they come back is a product call (distribute.you#3310). Remove their routes the same way once that call is made.

**`src/routes/admin.ts` is NOT in this class.** Its `SERVICE_DB_REGISTRY` maps a service name to a `*_DATABASE_URL` and opens a Postgres pool directly — it never calls the service. Six of the retired services still have a restored database on the box, so those entries keep working. Read what a route actually reaches before removing it by name.

### Brand-service path convention

- `/orgs/brands/*` (no ID in path) = org-scoped operations using `x-org-id` / `x-brand-id` headers (list, extract-fields, extract-images)
- `/internal/brands/{id}/*` = single-brand by-ID lookups (get brand, extracted-fields, extracted-images, runs)
- `/internal/brands?ids=csv` = batch by-ID lookup (proxied here as `GET /v1/brands/by-ids?ids=csv`). Preferred over fan-out `Promise.all` against the single-brand endpoint. Caller is responsible for staying within brand-service's per-request cap — if exceeded, brand-service returns 400 and the error is propagated verbatim. Do NOT add chunking, retry, or aggregation logic at this layer (rule #2). The dashboard owns batching across multiple requests when it has >cap brands to resolve.

### Service-client env var names must match the deployed service name

When adding a new entry to `externalServices` in `src/lib/service-client.ts`, the env var name MUST be `<SERVICE>_SERVICE_URL` / `<SERVICE>_SERVICE_API_KEY` where `<SERVICE>` matches the actual service / repo name in screaming snake case (e.g. `ai-visibility-score-service` → `AI_VISIBILITY_SCORE_SERVICE_URL`, NOT `AI_VISIBILITY_SERVICE_URL`).

Verify the var is actually present BEFORE shipping: env vars live on the box in `/root/distribute/env/<svc>.env`, so read this gateway's own file (`api-service.env`) and confirm the exact name you added is in it. Unit tests pass either way (they assert what the source contains, not what the environment has) — only a runtime call exposes the mismatch as a 500. Hotfix v0.36.1 fixed this for `ai-visibility-score-service` after dashboard 500s.

### Pagination params on list endpoints

Never add `.default()` or `.max()` on `limit` / `pageSize` / `per_page` / `count` query/body Zod schemas at the api-service layer. Callers get whatever the downstream service returns. Silent caps caused truncated-result bugs (outlets-service hotfix v0.2.1). Enforced by `tests/unit/no-limit-defaults.regression.test.ts` — it WILL fail your build if you add one.

### Diagnosing a "gateway route hangs / is slow but downstream serves fine"

The gateway is a pure passthrough: on a proxy route it does `callExternalService` → `res.json`, adding ~0 overhead when the downstream responds warm. So a report of "gateway times out but the downstream endpoint is fast directly" is almost never a gateway bug — it is downstream cold-state (request-path cache/dataset build, a container that just restarted after a deploy, a first-connection DB handshake). **Do NOT add a cache / warm-loop / per-route timeout / connection-prewarm to the gateway to "fix" it — that violates rules #2/#8 and would only surface the fallback faster, not the real data. The fix belongs downstream.**

The "direct is fast / gateway is slow" asymmetry is usually a **measurement artifact**: the first call (whichever path) warms the downstream's shared cache, so the *second-measured* path looks artificially fast. To diagnose correctly, control for warm-state:
- Measure the UNTOUCHED path FIRST after a genuine idle (past the downstream's cache TTL) — hit **direct, bypassing the gateway**; if direct-cold is also slow, the gateway is exonerated.
- The tell for a shared downstream cold build: N concurrent requests all **return together at the same elevated latency** (one cold `O(brands)`/`O(N)` fan-out they all block on), then everything is fast for the cache-TTL window.
- DB cold-start is no longer a candidate: every service's database is one always-on Postgres container on the box (`docker exec distribute-postgres-1 psql -U postgres -d <svc>_service`), with no per-branch compute that suspends. So a slow first call points at a request-path build in the downstream, not at its DB waking up — read the downstream's own repo issues before blaming the gateway.

Incident 2026-07-11 (public cost-per-outcome-trend "gateway timeout"): brief pinned it on the gateway; profiling showed gateway warm-overhead ~0, and a 5.5-min-idle direct-first A/B gave direct-cold ~11.6s / gateway-warm 0.32s — the gateway was innocent. Root cause was features-service's shared goal-bucket dataset cold-building on the request path (its own issue #547's documented next-lever); fixed there via SWR warm-off-request-path (features-service v0.87.7 / #551), no api-service change.

### Future direction

New proxy routes should follow the pattern `/v1/{service-name}/{original-downstream-path}` to make the mapping obvious and mechanical. Existing routes keep their current shape to avoid breaking clients.
